// Entry component kept for parity with CRA-style setups; routing lives in app/router.jsx
// and is rendered directly from main.jsx via <RouterProvider />.
export default function App() {
  return null;
}
